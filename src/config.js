// Єдиний перемикач для системи акаунтів (auth-freeze-brief.md): поки
// бекенд-синхронізація ігрового прогресу ще не готова, форма входу/
// реєстрації прихована, кнопка профілю показує бейдж "Скоро", а сторінка
// акаунта — стан "У розробці". Прогрес і далі повністю зберігається в
// localStorage, як і раніше — цей прапорець лише ховає ще не робочий UI
// входу, нічого не змінює в самому збереженні.
//
// VITE_AUTH_ENABLED=true в .env вмикає акаунти назад одним прапорцем,
// без розкиданих перевірок по десятках компонентів.
export const AUTH_ENABLED = import.meta.env.VITE_AUTH_ENABLED === "true";
