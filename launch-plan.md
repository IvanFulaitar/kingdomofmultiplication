# План готовності до релізу «Королівство Математики»

Це — головний, пріоритетний план дій. Він **строго діє наперед**: нові
візуальні фічі й додатковий контент (десятий режим, ще аватари, ще
досягнення) чекають, поки не закриті пункти нижче. Головна проблема не в
браку контенту, а в чотирьох речах:

1. Надійність прогресу.
2. Якість навчання.
3. Зрозумілий перший запуск.
4. Технічна, правова й продуктова готовність.

Пов'язані документи: `roadmap-kingdom-of-mathematics.md` (майбутнє
розширення математичних тем — дроби/рівняння/геометрія, довгостроково,
після цього плану); `rekomendovana_model_monetyzatsii_math_rpg.md`
(модель монетизації — теж довгостроково, після першого безкоштовного
релізу, див. розділ 27).

## Зміст

1. [Правильно позиціонувати перший реліз](#1-правильно-позиціонувати-перший-реліз)
2. [Що вже можна залишити для першого релізу](#2-що-вже-можна-залишити-для-першого-релізу)
3. [Найкритичніші завдання перед запуском](#3-найкритичніші-завдання-перед-запуском)
4. [Повністю переробити перші 5 хвилин гри](#4-повністю-переробити-перші-5-хвилин-гри)
5. [Перетворити тренування на справжню систему навчання](#5-перетворити-тренування-на-справжню-систему-навчання)
6. [Додати пояснення після помилки](#6-додати-пояснення-після-помилки)
7. [Урізноманітнити завдання в сюжетних рівнях](#7-урізноманітнити-завдання-в-сюжетних-рівнях)
8. [Додати окремий режим для дорослого](#8-додати-окремий-режим-для-дорослого)
9. [Збереження прогресу — головний технічний ризик](#9-збереження-прогресу--головний-технічний-ризик)
10. [Не поспішати з акаунтами](#10-не-поспішати-з-акаунтами)
11. [Обов'язкові сторінки перед публікацією](#11-обовязкові-сторінки-перед-публікацією)
12. [Доступність](#12-доступність)
13. [Продуктивність](#13-продуктивність)
14. [Зробити PWA](#14-зробити-pwa)
15. [Аналітика без порушення приватності](#15-аналітика-без-порушення-приватності)
16. [Стабільність і обробка помилок](#16-стабільність-і-обробка-помилок)
17. [Тестування перед запуском](#17-тестування-перед-запуском)
18. [Матриця реальних пристроїв](#18-матриця-реальних-пристроїв)
19. [Баланс економіки](#19-баланс-економіки)
20. [Щоденні завдання треба зробити різноманітнішими](#20-щоденні-завдання-треба-зробити-різноманітнішими)
21. [Досягнення також потрібно розширити](#21-досягнення-також-потрібно-розширити)
22. [Контентний редактор](#22-контентний-редактор)
23. [Архітектура, яку варто мати до масштабування](#23-архітектура-яку-варто-мати-до-масштабування)
24. [План запуску на 6 етапів](#24-план-запуску-на-6-етапів)
25. [Які метрики дивитися після запуску](#25-які-метрики-дивитися-після-запуску)
26. [Критерії готовності до публічної бети](#26-критерії-готовності-до-публічної-бети)
27. [Монетизація](#27-монетизація)

---

## 1. Правильно позиціонувати перший реліз

Назва «Королівство Математики» обіцяє багато різних математичних тем, але
зараз фактично доступна лише таблиця множення 2–9. Не потрібно чекати,
поки будуть готові всі 100+ рівнів. Першу версію краще представити так:

> **Королівство Математики**
> Розділ 1: Магія множення

або:

> **Королівство Математики: Таблиця множення**

На карті можна показати майбутні закриті світи:

- Місто Великих Чисел — скоро.
- Острів Дробів — скоро.
- Долина Рівнянь — скоро.
- Геометричний архіпелаг — скоро.

Так користувач бачить масштаб гри, але не відчуває, що йому пообіцяли
весь курс 5–6 класу, а дали тільки множення.

## 2. Що вже можна залишити для першого релізу

Для публічної бети достатньо:

- 12 сюжетних рівнів;
- 4 регіонів;
- 3 тренувальних режимів;
- 16 аватарів;
- 8 досягнень;
- щоденних завдань;
- XP, монет, рівнів героя;
- музики та звуків;
- адаптивного мобільного дизайну.

Не варто зараз терміново створювати 100 рівнів. Краще зробити наявні 12
рівнів такими, щоб вони:

- не повторювали одні й ті самі приклади;
- адаптувалися до слабких місць;
- пояснювали помилки;
- мали різні типи завдань;
- стабільно зберігали прогрес;
- добре працювали на реальних телефонах.

## 3. Найкритичніші завдання перед запуском

| Пріоритет | Що потрібно | Критерій готовності |
|---|---|---|
| P0 | Надійне збереження | Прогрес не губиться після оновлень, помилок і перезапусків |
| P0 | Перший запуск і навчання | Нова дитина без пояснень розуміє, що натискати |
| P0 | Обробка помилок | Жоден збій не залишає білий або завислий екран |
| P0 | Політика приватності | Зрозуміло, що зберігається і чи передаються дані |
| P0 | Мобільне тестування | Усе працює на вузьких Android та iPhone (кодовий аудит зроблено — розділ 18; реальні пристрої ще потрібні) |
| P1 | Адаптивне навчання | Слабкі приклади з'являються частіше |
| P1 | Пояснення помилок | Дитина бачить не лише «неправильно», а й спосіб розв'язання |
| P1 | Аналітика продукту | Видно, де користувачі виходять або помиляються |
| P1 | PWA | Гру можна встановити на телефон |
| P2 | Хмарне збереження | Прогрес переноситься між пристроями |
| P2 | Нові математичні світи | Дроби, рівняння, геометрія та інші теми |

## 4. Повністю переробити перші 5 хвилин гри

Зараз новий користувач, ймовірно, одразу бачить великий головний екран.
Для автора все зрозуміло, але дитина ще не знає:

- що означають зірки;
- навіщо монети;
- що дає XP;
- чим тренування відрізняються від карти;
- як отримати три зірки;
- що станеться після втрати трьох сердець.

### Рекомендований перший запуск

**Екран 1 — коротке знайомство**

> Ласкаво просимо до Королівства Математики!
> Розв'язуй завдання, перемагай охоронців і відкривай нові землі.

Кнопка: **Почати пригоду**

**Екран 2 — вибір старту**

Запитати не вік і не персональні дані, а навчальну впевненість:

- Я тільки вчу таблицю множення.
- Я вже трохи знаю.
- Я хочу перевірити себе.

**Екран 3 — коротка діагностика**

Дати 8–12 прикладів без покарань та життів. За результатом:

- визначити слабкі таблиці;
- запропонувати стартовий рівень складності;
- заповнити початкову статистику.

**Екран 4 — інтерактивний tutorial**

Показати один демонстраційний бій:

- Обери відповідь.
- Правильна відповідь завдає шкоди.
- Серця — твої спроби.
- За перемогу отримуєш зірки, монети й XP.

Tutorial має тривати максимум кілька дій, а не бути довгою інструкцією.

## 5. Перетворити тренування на справжню систему навчання

Зараз гра добре тренує швидке відтворення відповідей, але для повноцінного
навчального продукту потрібен не тільки drill.

### Для кожної пари множення зберігати

Наприклад, для 7 × 8:

```json
{
  "attempts": 14,
  "correct": 9,
  "incorrect": 5,
  "averageResponseTime": 4.2,
  "lastAnsweredAt": "...",
  "correctStreak": 2,
  "mastery": 61
}
```

### Система повинна враховувати

- відсоток правильних відповідей;
- швидкість відповіді;
- скільки часу минуло після останнього повторення;
- останні помилки;
- кількість правильних відповідей поспіль.

### Пріоритет прикладу

Умовно:

```
пріоритет =
  помилки
  + повільні відповіді
  + давно не повторювалося
  - стабільна серія правильних відповідей
```

Слабкі приклади мають з'являтися частіше, але не настільки часто, щоб
дитина бачила одне й те саме п'ять разів поспіль.

### Додати статус засвоєння

Для кожної таблиці:

- 🔴 Потрібно потренувати.
- 🟡 Майже засвоєно.
- 🟢 Добре знаю.
- ⭐ Майстер.

Це можна показати у спеціальному екрані «Мої знання».

## 6. Додати пояснення після помилки

Найбільша навчальна прогалина — відповідь «правильно/неправильно» без
пояснення.

Для прикладу 7 × 8 після другої помилки можна показати:

> 7 × 8 — це сім груп по вісім.
> 7 × 5 = 35
> 7 × 3 = 21
> 35 + 21 = 56

Або візуальну підказку:

```
●●●●●●●●
●●●●●●●●
●●●●●●●●
●●●●●●●●
●●●●●●●●
●●●●●●●●
●●●●●●●●
```

Види підказок:

- групи предметів;
- числова пряма;
- розкладання множника;
- сусідній відомий приклад;
- переставна властивість: 8 × 7 = 7 × 8.

Правильну відповідь не потрібно миттєво показувати після першої помилки.
Краще:

1. Дати повторну спробу.
2. Показати легку підказку.
3. Після наступної помилки пояснити розв'язання.

## 7. Урізноманітнити завдання в сюжетних рівнях

Навіть у межах таблиці множення можна зробити значно більше, ніж
стандартне `7 × 8 = ?`.

Рекомендований розподіл:

| Тип | Приклад |
|---|---|
| Звичайний приклад | 7 × 8 = ? |
| Пропущений множник | 7 × ? = 56 |
| Правда чи помилка | 8 × 6 = 46 |
| Порівняння | 6 × 8 чи 7 × 7 |
| Ланцюжок | 4 × 5 + 6 |
| Обернене завдання | 56 ÷ 7 |
| Текстова задача | 6 кошиків по 7 яблук |
| Сортування | розстав результати за зростанням |
| Пошук пари | 7 × 8 ↔ 56 |
| Вибір дверей | результат написаний на проході |

Рівень може складатися, наприклад, із:

- 50% звичайних прикладів;
- 20% пропущених чисел;
- 15% порівнянь;
- 15% коротких задач.

## 8. Додати окремий режим для дорослого

Для гри, яку можуть використовувати батьки або вчителі, дуже корисний
екран: **Прогрес навчання**. Він може працювати локально без акаунта.

Показувати:

- загальну точність;
- середній час відповіді;
- найсильніші таблиці;
- найслабші пари;
- кількість занять за тиждень;
- тривалість занять;
- прогрес за останні 7 і 30 днів;
- рекомендацію, що повторити.

Приклад:

> Варто повторити: 7 × 8, 6 × 9, 8 × 4

Додати:

- «Скинути прогрес» із підтвердженням.

(Ручний JSON-експорт/імпорт колись був тут, але прибраний після появи
акаунта (`AuthScreen.jsx`, `frontend-backend-integration-plan.md`) — тепер
це вирішує вхід в акаунт, а не ручний файл.)

Посилання на Instagram автора також краще винести в невеликий розділ
«Для дорослих / Про гру», а не робити його центральною дитячою дією.

## 9. Збереження прогресу — головний технічний ризик

localStorage підходить для невеликого локального прогресу, але воно
прив'язане до конкретного origin і браузера. Дані можуть зникнути після
очищення сховища, а в приватному режимі очищаються після завершення
приватної сесії. Обсяг Web Storage також обмежений; для самого прогресу
цього достатньо, але аудіо й великі ресурси там зберігати не потрібно.

### До запуску потрібно додати

**Версію збереження**

```json
{
  "saveVersion": 3,
  "createdAt": "...",
  "updatedAt": "...",
  "profile": {},
  "progress": {},
  "statistics": {}
}
```

**Міграції**

```js
migrateSave(save) {
  if (save.saveVersion === 1) {
    // migrate to v2
  }
  if (save.saveVersion === 2) {
    // migrate to v3
  }
  return save;
}
```

Інакше після зміни структури даних старі користувачі можуть втратити
прогрес або отримати помилки.

**Резервний snapshot**

Зберігати:

- `gameSaveCurrent`;
- `gameSaveBackup`.

Перед важливою зміною поточного збереження копіювати його в backup.

**Перевірку цілісності**

Якщо JSON пошкоджений:

1. Спробувати backup.
2. Показати зрозуміле повідомлення.
3. Не скидати все мовчки.
4. Запропонувати експортувати пошкоджені дані для діагностики.

**Експорт та імпорт**

Файл: `kingdom-math-save-2026-07-31.json`

Це частково розв'яже проблему перенесення між пристроями до появи
акаунтів.

## 10. Не поспішати з акаунтами

Для першого публічного релізу залишити:

- без реєстрації;
- без імені;
- без email;
- без чату;
- без публічного рейтингу;
- без реклами;
- без персоналізованих рекламних трекерів.

Це значно спрощує запуск дитячого продукту.

Якщо сервіс орієнтований на дітей до 13 років і збирає персональні дані,
можуть застосовуватися вимоги COPPA, зокрема повідомлення батькам і
перевірювана батьківська згода. У ЄС для обробки дитячих даних на основі
згоди віковий поріг залежить від країни та перебуває в діапазоні 13–16
років; інформація для дітей має бути викладена простою та зрозумілою
мовою.

**Це не юридична консультація**, але безпечний перший варіант — не
збирати персональні дані взагалі, доки немає чіткої потреби.

## 11. Обов'язкові сторінки перед публікацією

**Реалізовано.** Чотири статичні сторінки — `public/about.html`,
`public/privacy.html`, `public/terms.html`, `public/contact.html` —
навмисно окремі `.html`-файли поза React-роутингом, щоб мати стабільні
публічні URL (`/privacy.html` тощо), незалежні від збірки SPA; саме
такий стабільний HTTPS-URL і потрібен для Data Safety/Privacy Policy в
Google Play Console (`android-play-store-plan.md`, розділ 11). Лінк на
всі чотири — внизу головного екрана (`MenuScreen.jsx`, під підписом
автора), відкриваються в новій вкладці. Контакти: email
fulaytar2405@gmail.com + Instagram @ivan_stepanowich.

Навіть без акаунтів додати:

**Про гру**

- що це за гра;
- кому вона призначена;
- хто автор;
- які теми доступні;
- що гра не замінює шкільного вчителя.

**Політика приватності**

Простими словами:

- прогрес зберігається локально;
- немає акаунтів;
- які технічні дані збираються;
- чи використовуються cookies;
- чи є аналітика;
- як зв'язатися з автором;
- як видалити дані.

**Умови використання**

- гра надається як навчальний інструмент;
- авторські права на графіку, музику та код;
- правила використання.

**Контакти**

Instagram можна залишити, але бажано також мати email для:

- повідомлень про баги;
- пропозицій учителів;
- звернень батьків;
- питань про приватність.

## 12. Доступність

Для публічного запуску варто орієнтуватися на WCAG 2.2 AA. Мінімальний
контраст звичайного тексту має бути 4.5:1; інтерактивні елементи повинні
мати видимий keyboard focus, логічний порядок фокусу та достатню область
натискання. Для власного внутрішнього стандарту краще робити кнопки не
меншими за 44×44 CSS px, що перевищує мінімальну вимогу WCAG AA й
зручніше для дітей на сенсорних екранах.

Перед запуском перевірити:

- керування клавіатурою;
- aria-label для кнопок-іконок;
- видимий focus-visible;
- контраст золотого тексту;
- контраст приглушених завершених завдань;
- правильний порядок читання screen reader;
- відсутність інформації лише через колір;
- режим prefers-reduced-motion;
- можливість вимкнути музику і звуки.

Анімації, що виникають після взаємодії, бажано вимикати або суттєво
скорочувати для користувачів із prefers-reduced-motion.

**Частково реалізовано (04.08.2026) — кодовий аудит.** Загалом
дизайн-система вже мала непогану базу (aria-label на більшості кнопок-
іконок, видимі focus-visible кільця на основних кнопках, форма входу з
aria-invalid/aria-describedby/role="alert"), тож основна робота — знайти
й закрити конкретні прогалини:

- `prefers-reduced-motion` раніше вимикав анімацію лише в кількох
  вибраних класах (auth-spinner, кілька ефектів на екрані результатів
  знань) — легко забути новий `@keyframes` при додаванні фічі. Додано
  один глобальний запобіжник (`*, *::before, *::after` →
  `animation-duration`/`transition-duration: 0.01ms !important` у
  `prefers-reduced-motion: reduce`), який покриває взагалі всі анімації
  гри одразу, без потреби перелічувати класи вручну. Жодна логіка в JS
  не чекає на `animationend`/`transitionend`, тож це безпечно.
- `.legal-footer-link:focus-visible` розрізняв фокус лише кольором тексту
  (без видимого кільця) — додано `box-shadow`-кільце, як в інших
  інтерактивних елементів.
- `LanguagePickerModal.jsx`, `BadgesModal.jsx` — не мали `role="dialog"`/
  `aria-labelledby` і не закривались по Escape; переведені на спільний
  `useModalDialog` (той самий хук, що вже використовували install-
  модалки — Escape, фокус на перший елемент, повернення фокусу, id для
  aria-labelledby).
- `ExitConfirmModal.jsx` (підтвердження виходу з рівня) — навмисно не
  закривається кліком по фону (щоб не втратити прогрес випадковим тапом
  повз кнопки), але Escape усе одно мав спрацьовувати як найбезпечніша
  дія — раніше не спрацьовував. Додано (мапиться на "Продовжити", а не
  на вихід).
- `MemoryScreen.jsx` — кнопка "назад" (←) була без `aria-label`, хоча
  такі самі кнопки в GameScreen/MazeScreen/RaceScreen уже мали. Вирівняно.
- Контраст: порахував за формулою WCAG кілька приглушених текстових
  класів на реальному фоні (`#1e1b4b`). `text-violet-300/60`
  (auth-screen підказка, "прогрес зберігається" підпис у налаштуваннях)
  давав 4.02:1 — трохи нижче порогу 4.5:1 для звичайного тексту; підняв
  до `/80` (6.04:1). Приглушений текст заблокованих рівнів на карті
  (`text-white/40`, 3.67:1) НЕ чіпав — це текст усередині `disabled`-
  кнопки, а неактивні елементи керування прямо виключені з вимоги
  контрасту WCAG 1.4.3.

Не встигли: повний скрін-рідер прогін (VoiceOver/TalkBack) реальними
руками — це вже не можна перевірити кодом, тільки на пристрої.

## 13. Продуктивність

Гра має багато PNG, фонів, аватарів, музики й звуків. На слабкому
телефоні це може спричинити:

- довгий перший запуск;
- зависання під час переходів;
- запізнення звуку;
- стрибки інтерфейсу;
- велике використання пам'яті.

Орієнтири хорошої вебпродуктивності: LCP не більше 2.5 секунди, INP не
більше 200 мс і CLS не більше 0.1 для більшості користувачів.

Що зробити:

- оптимізувати всі PNG;
- генерувати кілька розмірів великих фонів;
- використовувати WebP/AVIF для непрозорих пейзажів;
- залишити PNG для зображень із прозорістю;
- lazy-load аватари магазину;
- не завантажувати всі 36 звуків на першому екрані;
- завантажувати звуки режиму перед його запуском;
- preload лише головного фону, активного аватара і базових UI-іконок;
- резервувати розміри зображень, щоб блоки не стрибали;
- розділити JS по маршрутах;
- уникати важких blur та glow на великій кількості елементів одночасно;
- тестувати FPS на реальному бюджетному Android.

Частково реалізовано (04.08.2026), код-рівневий аудит без реального
Lighthouse/пристрою (build tooling недоступний у цій сесії):

- **WebP для непрозорих фонів**: 5 великих непрозорих фонів (app_bg,
  A/B/C/D — разом ~4.3 МБ PNG) конвертовано у WebP (~307 КБ разом,
  економія ~93%). PNG лишились поруч як резервний варіант. Підключено
  без жодного JS через подвійне оголошення `background-image` у CSS
  (`.bg-app-main`, `.bg-region-A/B/C/D` у `src/index.css`) — браузер, що
  не розуміє `image-set()`, автоматично лишається на PNG-варіанті, який
  оголошено першим; сучасні браузери підхоплюють WebP. Замінено inline
  `style={{backgroundImage:...}}` на ці класи в App.jsx, GameScreen.jsx,
  MapScreen.jsx, ResultsScreen.jsx. Мініатюра регіону A в
  OnboardingScreen.jsx (48×48) також переведена на `.webp` (ArtImage вже
  має власний `onError`-fallback на emoji, тож ризику немає). AVIF не
  додано — WebP вже дає майже весь виграш стиснення без ризику
  сумісності, а генерація AVIF без збірних інструментів у цій сесії
  недоступна.
- **Кілька розмірів великих фонів**: НЕ зроблено (потребує responsive
  `srcset`/кількох ширин під різні viewport — окрема задача, не
  критична, бо фони й так тепер ~15-16 КБ/шт у WebP).
- **PNG лишається для прозорих зображень**: без змін — аватари/іконки з
  прозорістю (магазин, значки) і далі PNG, конверсія в WebP там не
  чіпалась (не непрозорі пейзажі).
- **Lazy-load аватарів магазину**: додано `loading="lazy"` в
  `ArtImage.jsx` (новий проброшений проп) і застосовано на сітці 15
  аватарів у ShopScreen.jsx (разом ~3.9 МБ) та на сітці бейджів у
  BadgesModal.jsx — офскрін-картки більше не тягнуться одразу при
  відкритті екрана/модалки.
- **Аудіо-стратегія (36 звуків, звуки режиму)**: перевірено — вже
  реалізовано раніше, без змін. `preloadCoreSfx()` вантажить лише
  базовий підмножина на старті; `preloadSfxGroup("maze"|"memory"|"race"|
  "combat")` реально викликається з кожного відповідного екрана
  (MazeScreen.jsx, MemoryScreen.jsx, RaceScreen.jsx, GameScreen.jsx)
  перед запуском режиму. `music.js` фетчить лише ОДИН аудіо-формат
  (ogg → mp3 fallback), ніколи обидва, і робить це нелокуюче через
  `useEffect`.
- **Preload лише головного фону + активного аватара**: `index.html`
  вже мав preload app_bg + wizard.png; тепер preload app_bg переведено
  на `app_bg.webp` з `type="image/webp"` — браузери без підтримки WebP
  просто ігнорують цей preload (за специфікацією `type` на
  `<link rel=preload>`), фактичний фон все одно підвантажиться через
  CSS-фолбек. "Активний аватар" і далі статично preload-иться як
  wizard.png (перший/дефолтний аватар) — динамічний preload під
  реального обраного аватара користувача вимагав би SSR або
  runtime-ін'єкції тегу, не зроблено.
- **Резервування розмірів зображень (CLS)**: перевірено вибірково — усі
  ArtImage/фонові елементи в сітках (магазин, бейджі, топбар) вже мають
  фіксовані Tailwind-розміри (`w-X h-X`), тож браузер резервує місце до
  завантаження картинки; повного пооб'єктного аудиту кожного `<img>` не
  проведено.
- **Розділення JS по маршрутах**: перевірено — вже реалізовано раніше,
  без змін. Усі важкі екрани (`OnboardingScreen`, `ShopScreen`,
  `TrainingScreen`, `MemoryScreen`, `MazeScreen`, `RaceScreen`,
  `RaceDifficultyScreen`, `MapScreen`, `AuthScreen`, `GameScreen`,
  `ResultsScreen`, `BadgesModal`) підключені через `React.lazy()` у
  `App.jsx`.
- **Blur/glow на багатьох елементах одночасно**: перевірено — усі
  `filter: blur()`/glow-класи в `index.css` (`.staff-glow`,
  `.combatant-glow-hero/enemy`, `.enemy-stand-glow`, модальні backdrop
  blur) застосовуються по одному екземпляру на екран, а не на сітках
  елементів (клітинки лабіринту, наприклад, не мають per-cell blur) —
  проблеми не знайдено, фікс не потрібен.
- **FPS на реальному бюджетному Android**: недоступно перевірити в цій
  пісочниці — залишається відкритим пунктом для ручного тестування.



Для такої гри PWA дуже доречна:

- користувач може встановити її на домашній екран;
- гра відкривається без браузерної панелі;
- базові ресурси можна кешувати;
- частину гри можна запускати без інтернету.

Для встановлюваної PWA потрібен Web App Manifest; service worker можна
використати для offline-сторінки та кешування критичних ресурсів.

Додати:

- `manifest.webmanifest`
- `service-worker.js`
- `icons/icon-192.png`
- `icons/icon-512.png`
- `icons/maskable-512.png`

Також:

- `theme_color`;
- `background_color`;
- `display: standalone`;
- offline fallback;
- екран «Доступне оновлення»;
- кнопку «Встановити гру», коли браузер це підтримує.

Не кешувати старий JS назавжди. Потрібна контрольована стратегія
оновлення, щоб користувачі не залишалися на застарілій версії.

**Реалізовано.** `public/manifest.webmanifest` (name/short_name, icons
192/512/maskable-512, `theme_color`/`background_color` #1e1b4b,
`display: standalone`), підключено в `index.html` разом із
`<meta name="theme-color">`. `public/service-worker.js` — рукописний, без
build-плагіна: navigation — "network first" (завжди свіжий index.html,
коли є мережа, кеш лише як fallback — вимога "не кешувати старий JS
назавжди" виконана явно), `/assets/*` + шрифти — "stale while
revalidate". `public/offline.html` — fallback-сторінка, коли взагалі
немає ні мережі, ні кешу. Оновлення НЕ застосовується автоматично: нова
версія чекає в стані "waiting", `UpdateBanner.jsx` показує
"Доступне оновлення" з кнопкою "Оновити" (шле `SKIP_WAITING`, чекає
`controllerchange`, тоді reload) — щоб дитину не перекинуло посеред
бою.

Сценарій встановлення (технічне ТЗ від 04.08.2026) — жодних технічних
термінів на екрані (не "beforeinstallprompt", не "PWA", не "manifest", не
"не підтримується"), лише прості формулювання. Уся розгалужена логіка
винесена в `src/hooks/useInstallFlow.js`, яким користуються і постійна
кнопка в попапі налаштувань (`MenuScreen.jsx`), і ненав'язливий банер
після перемоги (`InstallBanner.jsx`, рендериться в `ResultsScreen.jsx`):

- **Android/Chromium** — подія `beforeinstallprompt` (лише в
  `src/game/pwa.js`, `preventDefault()` + збереження, ніякого автопопапу)
  вмикає кнопку "Встановити гру"; клік викликає системний діалог
  (`event.prompt()`); після `appinstalled` кнопка зникає.
- **iOS Safari** — такої події не існує в принципі, тож кнопка веде на
  `IosInstallModal.jsx`: побутова покрокова інструкція "Поділитися →
  На початковий екран → Додати" зі схематичною ілюстрацією (не скриншот
  Safari, щоб не застаріти), кнопки "Зрозуміло"/"Не зараз".
- **iOS, але не Safari** (Chrome/Instagram-вебвʼю тощо — виявляється по
  UA-токенах `CriOS`/`FxiOS`/`Instagram`/... в `isIosSafariBrowser()`) —
  там пункту "На початковий екран" часто взагалі нема, тож
  `OpenInSafariModal.jsx` пропонує спершу скопіювати посилання
  (`navigator.clipboard`, з textarea+`execCommand` фолбеком для
  вебвʼю, де clipboard API заблоковано) і відкрити сторінку в Safari.
- **Вже встановлено** (`isAppInstalled()` — `display-mode: standalone`
  або iOS-специфічний `navigator.standalone`) — кнопка, банер і всі
  модалки просто не рендеряться.
- Без окремого банера — лише постійна кнопка "Встановити гру" в попапі
  налаштувань (`canShowInstallSuggestion()`/`dismissInstallSuggestion()`
  у `src/game/pwa.js` лишились у коді про запас, але зараз нічого не
  викликає їх — банер на головному екрані прибрали за прямим проханням).
- Модалки: `role="dialog"`, `aria-modal`, `aria-labelledby`, Escape на
  десктопі, фокус повертається на кнопку-тригер, внутрішня прокрутка на
  малих екранах (`max-h-[88dvh] overflow-y-auto`) — спільна логіка в
  `src/hooks/useModalDialog.js`.

## 15. Аналітика без порушення приватності

Без аналітики після запуску не буде видно:

- чи проходять tutorial;
- де виходять;
- який рівень занадто складний;
- який режим майже не запускають;
- які приклади найважчі;
- чи повертаються наступного дня.

### Мінімальні події

- `app_open`
- `tutorial_started`
- `tutorial_completed`
- `diagnostic_completed`
- `level_started`
- `level_completed`
- `level_failed`
- `answer_submitted`
- `hint_used`
- `avatar_purchased`
- `training_started`
- `race_difficulty_selected`
- `race_finished`
- `save_exported`
- `app_error`

Для відповідей:

```json
{
  "skillId": "multiply_7_8",
  "correct": false,
  "responseTimeBucket": "3-5s",
  "mode": "battle",
  "levelId": "forest_gate"
}
```

Не передавати:

- ім'я дитини;
- дату народження;
- точну адресу;
- контакти;
- текстові повідомлення;
- рекламний ідентифікатор.

Найбезпечніший старт — агрегована або self-hosted аналітика без
рекламних профілів. Якщо додається будь-яке відстеження користувачів,
його потрібно окремо перевірити з погляду правил дитячої приватності.

**Реалізовано (лише фронтенд).** `src/config.js` — `ANALYTICS_ENABLED`
(за замовчуванням `false`, той самий підхід, що й `AUTH_ENABLED`) і
окремий `ANALYTICS_ENDPOINT` (навмисно НЕ той самий бекенд, що
`VITE_API_URL` — акаунти/синхронізація прогресу й далі заморожені за
попереднім рішенням, аналітика від них не залежить і не вимагає
входу). `src/game/analytics.js` — `trackEvent()` є повним no-op, поки
прапорець вимкнено (нічого не пишеться навіть у localStorage); коли
увімкнено — події чергуються локально (з обмеженням розміру) і батчами
йдуть на `ANALYTICS_ENDPOINT` (fetch з ретраєм, `sendBeacon` на
`pagehide`/приховану вкладку); без заданого endpoint черга просто
накопичується локально й нікуди не йде. Підключено 14 із 15 подій зі
списку вище: `app_open`, `tutorial_started`, `tutorial_completed`,
`diagnostic_completed`, `level_started`, `level_completed`,
`level_failed`, `answer_submitted` (skillId/correct/
responseTimeBucket/mode/levelId — без точного часу), `hint_used`,
`avatar_purchased`, `training_started`, `race_difficulty_selected`,
`race_finished`, `app_error` (лише узагальнена назва класу помилки,
без message/stack). `save_exported` — НЕ підключено: наразі в
застосунку немає UI для експорту збереження у файл (сама функція
export/import ще не винесена на жоден екран), тож тригера для цієї
події поки не існує.

Щоб система реально запрацювала: (1) підняти окремий self-hosted чи
агрегований сервіс прийому подій, (2) виставити
`VITE_ANALYTICS_ENDPOINT` і `VITE_ANALYTICS_ENABLED=true` в `.env`, (3)
окремо перевірити цей сервіс із погляду дитячої приватності, як і
зазначено вище.

## 16. Стабільність і обробка помилок

Додати глобальний ErrorBoundary. Замість білого екрана:

> Щось пішло не так
> Твій прогрес збережено. Спробуй перезапустити гру.

Кнопки:

- Перезапустити.
- Повернутися на головну.
- Скопіювати код помилки.

Обробити:

- невдале завантаження зображення;
- невдале завантаження аудіо;
- пошкоджений save;
- `QuotaExceededError`;
- невдалий service worker;
- відсутність мережі;
- невідомий ID аватара;
- старий формат рівня;
- подвійне натискання покупки;
- вихід зі сторінки під час запису прогресу.

Для перевірки веббезпеки можна використовувати OWASP ASVS як список
технічних вимог і OWASP Web Security Testing Guide як основу для
тестування.

Частково реалізовано (04.08.2026), код-рівневий аудит без реального
пристрою/автотестів (недоступні в цій сесії):

- **ErrorBoundary, пошкоджений save, backup/відновлення**: без змін —
  вже реалізовано раніше (progress.js: loadProgress/saveProgress/
  migrateSave з BACKUP_KEY + CORRUPTED_KEY, ErrorBoundary.jsx).
- **QuotaExceededError**: раніше `saveProgress()` мовчки ковтала будь-яку
  помилку запису без жодного фідбеку гравцю — прогрес у пам'яті лишався
  цілим, але на диск нічого не писалось, і про це ніхто не дізнавався.
  Тепер `saveProgress()` повертає true/false; `App.jsx: persist()` при
  невдалому записі показує SaveNoticeToast (причина "save-failed"), не
  частіше одного разу за сеанс (щоб не спамити тим самим тостом на
  кожну наступну дію, поки місце не звільниться).
- **SaveNoticeToast.jsx НЕ була перекладена**: на відміну від усіх інших
  toast-компонентів (BadgeToast, UpdateBanner), цей мав захардкожений
  український текст — гравець з іншою мовою інтерфейсу бачив
  україномовне попередження. Перекладено через `common.json` (нові
  ключі `saveRecoveredTitle/Body`, `saveResetTitle/Body`,
  `saveFailedTitle/Body`, `gotIt`) у всіх 8 мовах.
- **Невдале завантаження зображення**: без змін — ArtImage.jsx вже має
  власний `onError`-fallback на emoji для кожного зображення гри.
- **Невдале завантаження аудіо**: без змін — sfx.js (`loadOne`) і
  music.js (`loadBuffer`) вже мають try/catch на кожен формат/файл і
  тихо повертають null замість падіння; playSfx()/ensurePlaying()
  просто нічого не програють, якщо буфер не завантажився.
- **Невдалий service worker**: без змін — `registerServiceWorker()` у
  pwa.js вже має `.catch()` з коментарем, що офлайн-кешування просто не
  запрацює цього разу, гра лишається звичайним сайтом.
- **Відсутність мережі**: без змін — apiClient.js вже кидає ApiError з
  code="NETWORK_ERROR" і зрозумілим повідомленням при недоступному
  сервері; основний ігровий прогрес не залежить від мережі взагалі
  (тільки акаунт, який необов'язковий).
- **Невідомий ID аватара**: перевірено — кожне місце, де читається
  `progress.avatar`/`avatarId` (MenuScreen, AuthScreen, GameScreen,
  MazeScreen, RaceScreen), вже має `AVATARS.find(...) ?? фолбек`, тож
  видалений/невідомий аватар просто показує дефолтний портрет/emoji, а
  не ламає екран.
- **Старий формат рівня**: перевірено — усі місця читання
  `progress.levels[id]` вже йдуть через `?.stars ?? 0`, формат ніколи
  фактично не змінювався відколи існує гра, окремої версійної міграції
  не знадобилось.
- **Подвійне натискання покупки**: перевірено — AvatarPurchaseModal.jsx
  вже має `loading`-стан і `disabled` на кнопці "Придбати" між кліком і
  завершенням `onConfirm()`, повторний клік ігнорується.
- **Вихід зі сторінки під час запису прогресу**: перевірено — `persist()`
  в App.jsx викликає `saveProgress()` синхронно одразу після
  `setProgress()`, без debounce/async розриву, тож до моменту, коли
  користувач фізично встигне закрити вкладку, запис у localStorage вже
  завершений.

## 17. Тестування перед запуском

### Автоматичні unit-тести

Покрити:

- генерацію прикладів;
- відсутність неправильних «правильних» відповідей;
- формування distractors;
- розрахунок зірок;
- XP та level-up;
- нагороди;
- покупки;
- міграції save;
- адаптивну складність;
- race rubber-banding;
- щоденні завдання;
- streak;
- блокування повторного отримання нагороди.

### Integration-тести

Сценарії:

1. Новий користувач проходить tutorial.
2. Перемагає на рівні.
3. Отримує XP і монети.
4. Купує аватар.
5. Перезапускає сторінку.
6. Увесь прогрес залишається.
7. Відкриває новий регіон.
8. Проходить тренування.
9. Отримує досягнення.
10. Забирає щоденну нагороду лише один раз.

### Обов'язкові негативні кейси

- подвійний клік на «Придбати»;
- перезавантаження під час нагороди;
- закриття вкладки під час бою;
- вихід через Android Back;
- зміна системного часу;
- localStorage недоступний;
- звук заблокований браузером;
- зображення не завантажилось;
- користувач відкрив старе збереження;
- користувач очистив частину даних;
- швидке багаторазове натискання відповіді.

Частково реалізовано (04.08.2026): справжній test-раннер (vitest/jest)
встановити не вдалось — npm-реєстр у цьому середовищі повертає 403 на
будь-який НОВИЙ пакет (лише те, що вже було в node_modules/lockfile,
доступне). Замість цього:

- `tests/logic.test.mjs` — 34 unit-тести на чисту ігрову логіку (без
  React/DOM), крихітний вбудований harness (`test`/`assert`/`assertEqual`,
  без залежностей), напряму імпортує СПРАВЖНІ модулі гри (не копії/мокі):
  `progress.js` (defaultProgress, migrateSave зі старого/порожнього
  прогресу, starsForMistakes, heroLevelFromXp з перевіркою монотонності на
  135 точках, checkQuests — включно з явним тестом на подвійне отримання
  нагороди, updateStreak — послідовні дні/розрив/той самий день,
  ensureDaily, save/load round-trip через in-memory localStorage-поліфіл,
  відновлення з backup при пошкодженому основному записі, чистий старт
  при пошкоджених обох, `saveProgress()` -> false при
  QuotaExceededError, чемпіонський заїзд і рекомендація складності);
  `generateQuestion.js` (factKey, timeForLevel, buildFactQuestion — повний
  перебір усіх 64 пар a×b від 2 до 9 на коректність/унікальність/
  відсутність від'ємних варіантів, getWeakFacts, factsUsedIn, і головне —
  1800 випадково згенерованих питань (150 × 12 рівнів) з перевіркою
  "рівно одна правильна відповідь серед унікальних варіантів" для
  КОЖНОГО типу питання, включно з рідкісними гілками comparе/missing/
  combined, де раніше вже траплялись реальні баги зависання циклів);
  `mastery.js` (computeMastery, masteryStatus — межі тіерів і окрема
  перевірка, що "Майстер" вимагає не лише відсоток, а й серію та
  швидкість, tableFacts, recommendTable — з явною перевіркою, що реальна
  слабкість переважає над просто невивченим); `raceEngine.js`
  (streakBonus, opponentGain — "гумове" наздоганяння завжди обмежене
  (200 випадкових прогонів з екстремальним розривом), starsForRace,
  rankParticipants — розбір нічиєї за часом відповіді, computeRaceReward —
  захист від фарму тренувального заїзду після 3 перемог на день, мінімум
  1 монета/1 XP завжди); `rewards.js` (pickDailyQuestIds — детермінованість
  і гарантія "одне easy + одне medium + одне training" на будь-яку дату).
- Під час першого прогону тести одразу знайшли 2 реальні проблеми в
  САМИХ тестах (не в грі) — неправильне припущення "завжди 4 варіанти"
  (питання типу "compare" мають рівно 2) і неврахований перетин фактів
  між сусідніми таблицями в mastery.js (факт "5×7" належить одразу й
  таблиці 5, і таблиці 7) — обидва виправлено в тестах, ігровий код не
  чіпався.
- Запуск: `npm test` (`scripts/run-tests.mjs`). Оскільки Vite-специфічний
  синтаксис (`import x from "./y.json"` без `with {type:"json"}`,
  використаний у src/i18n/index.js) не проходить у голому Node без
  збірки — написано мінімальний Node ESM loader-хук
  (`scripts/json-loader.mjs`, через `node:module` register()), що дає
  той самий ефект, що й Vite/браузер, без жодних правок production-коду.
  Спроба зібрати той самий тест через esbuild (уже присутній у
  node_modules) провалилась — node_modules у цьому середовищі
  встановлений під win32-x64 (з реального Windows-пристрою користувача),
  а нативний бінарник esbuild платформо-залежний і не працює під
  Linux-пісочницею; loader-хук цю проблему повністю обходить, бо не
  потребує нативного коду.
- НЕ зроблено (потребує браузера/DOM, недоступно в цій сесії):
  integration-тести (повний user-flow через реальні екрани),
  негативні кейси, що вимагають справжнього браузера (заблокований звук,
  Android Back, зміна системного часу пристрою), і сам звук/музика
  (sfx.js/music.js — Web Audio API, немає AudioContext у Node).

## 18. Матриця реальних пристроїв

Мінімум перевірити:

| Категорія | Варіанти |
|---|---|
| Ширина | 320, 360, 375, 390, 430 px |
| Android | Chrome, Samsung Internet |
| iOS | Safari |
| Desktop | Chrome, Firefox, Edge, Safari |
| Планшет | Android tablet, iPad |
| Орієнтація | Portrait, landscape |
| Мережа | Wi-Fi, повільний 4G, offline |
| Звук | Увімкнений, вимкнений, заблокований autoplay |
| Ввід | Touch, mouse, keyboard |

Особливу увагу приділити:

- safe-area;
- обрізанню сердець;
- довгим українським назвам;
- модалкам;
- прокручуванню магазину;
- картам 4×4;
- клавіатурі браузера;
- нижній навігаційній панелі Android/iOS.

**Частково реалізовано (04.08.2026) — кодовий аудит замість реальних
пристроїв** (це середовище не має доступу до фізичних телефонів, тож
справжнє ручне тестування на матриці вище все ще потрібне окремо;
зроблено те, що можна перевірити й виправити по коду):

- `index.html` і всі 5 статичних сторінок (`about/privacy/terms/contact/
  offline.html`) — у viewport-меті бракувало `viewport-fit=cover`, через
  що всі вже наявні `env(safe-area-inset-*)` у коді (MenuScreen,
  OnboardingScreen, RaceDifficultyScreen, WeakPracticeScreen) фактично
  завжди резолвились у 0 — на "чубатих" iPhone у встановленому режимі
  (`display: standalone` + `black-translucent` статус-бар) контент міг
  залазити під вирізку/індикатор. Виправлено.
- Bottom-sheet модалки (`LanguagePickerModal`, `IosInstallModal`,
  `OpenInSafariModal`, `BadgesModal`) — нижній відступ не враховував
  `safe-area-inset-bottom`, кнопки могли опинитись впритул до смуги
  жесту "додому". Додано `pb-[max(1.5rem,env(safe-area-inset-bottom))]`
  (лише на мобільній `items-end`-розкладці, на `sm:` — назад до
  звичайного відступу).
- Toast-компоненти, закріплені до країв екрана (`UpdateBanner`,
  `SaveNoticeToast` — низ; `SimpleToast`, `BadgeToast` — верх) — так само
  не враховували safe-area, виправлено.
- Замалий тап-таргет: кнопка закриття картки "прогрес зберігається"
  (24×24px → 36×36px), X-кнопка `AvatarPurchaseModal` (32×32px →
  40×40px), перемикачі "Музика"/"Звуки" в налаштуваннях — раніше сам
  повзунок був єдиною клікабельною зоною (44×24px), тепер клікабельний
  увесь рядок (як інші пункти того ж попапу).
- Ігрові тап-зони (клітинки лабіринту, картки пам'яті, кнопки відповідей
  битви, картки перегонів) уже мали з запасом ≥64px — без змін.
- Текстові поля (`AuthScreen`) уже на `text-base` (16px) — безпечно від
  автозуму iOS Safari при фокусі, без змін.
- `overflow-x: hidden` на `html/body` вже стоїть глобально — це
  запобіжник від горизонтального скролу навіть якщо якийсь елемент
  (напр. банер "Щоденні завдання" з `whitespace-nowrap` у довших мовах
  типу угорської/німецької) візуально не поміститься на 320px екрані.
- НЕ чіпали: паттерн блокування прокрутки модалок через
  `body.style.overflow = "hidden"` — на iOS Safari це не ідеально
  (можливий незначний bounce фону), але це вже наявний, широко
  використовуваний паттерн у багатьох модалках; переписувати його на
  `position: fixed` + збереження scrollY — це окрема, ризикованіша задача
  поза межами цього аудиту.

## 19. Баланс економіки

Потрібно створити таблицю всіх джерел і витрат монет.

**Джерела**

- проходження рівня;
- зірки;
- daily quests;
- перегони;
- лабіринт;
- досягнення;
- streak;
- бонусні скрині.

**Витрати**

- аватари;
- рамки;
- підказки, якщо плануються;
- косметичні ефекти;
- майбутні теми профілю.

Перевірити:

- скільки приблизно проходжень потрібно для дешевого аватара;
- скільки для найдорожчого;
- чи не можна купити все за один вечір;
- чи не потрібні тижні для першої покупки;
- чи не дають перегони непропорційно багато;
- чи не фармиться daily reward зміною системного часу.

Для першого релізу економіка має бути щедрою. Дитина повинна відносно
швидко отримати першу покупку, щоб зрозуміти сенс монет.

Реалізовано (04.08.2026) — таблиця з реальних чисел коду (не оцінка):

**Джерела монет (перевірено по коду):**

- Рівень (App.jsx:completeLevel) — `coinGain = max(0, нові_зірки − старі_зірки) × 10`.
  Це означає: монети йдуть ЛИШЕ за покращення результату понад
  попередній найкращий, повторне проходження вже "3-зіркового" рівня
  дає 0 монет (лише XP) — свідомий захист від нескінченного фарму
  через повтор бою. Перше проходження: 10 (1 зірка) / 20 (2) / 30 (3).
- Щоденні завдання (rewards.js: QUEST_POOL) — 3/день, по одному з
  easy/medium/training, 10-30 монет кожне -> 30-90/день сумарно.
- Перегони (raceEngine.js: computeRaceReward, raceDifficulties.js) —
  повна нагорода за 1 місце: Тренувальний ≈15, Пригодницький ≈38,
  Чемпіонський ≈80 (+ бонуси за точність/рекорд, макс +10). 2/3 місце —
  65%/35% від цього. Лише Тренувальний має захист від фарму: після 3
  перемог за день монети (не XP) множаться на 0.6.
- Лабіринт (MazeScreen.jsx) — `20 + tier×10 + зібрані_монети` (монетки
  5, скрині 15, секрет 25 за клітинку), БЕЗ обмеження на повторні
  проходження.
- Пам'ять (MemoryScreen.jsx) — фіксовано 20 монет/20 XP за проходження,
  теж без обмеження.
- Онбординг (App.jsx:completeOnboarding) — одноразово +15 монет.
- Досягнення/streak — НЕ дають монет (лише іконка-престиж); "бонусні
  скрині" з розділу плану — це `bonusChestChance: 0.25` на
  Чемпіонському заїзді (RaceScreen.jsx), не окреме окреме джерело.

**Витрати:** лише аватари (cosmetics.js, 15 штук + безкоштовний wizard).
Рамки/косметичні ефекти/теми профілю з плану — поки не реалізовані,
витрачати монети більше нема на що.

**Відповіді на конкретні питання з плану:**

- Найдешевший аватар — Лицар (Knight), 50 монет. Найдорожчий — Голем
  (Golem), 200 монет. Разом усі 15 (без wizard) — 1900 монет.
- Скільки проходжень для найдешевшого: 1-2 щоденних завдання АБО 2-3
  перших рівні (перфект) уже покривають 50 монет — перша покупка
  відбувається дуже швидко, у межах першої сесії, як і вимагає план.
- Найдорожчий (200): один вечір із кількома новими рівнями + щоденні
  завдання + одна перемога в перегонах реалістично дає ~150-200 монет
  разом — досяжно за 1-2 сесії, не тижні.
- Чи можна купити ВСЕ за один вечір: ні, і це навмисно — 1900 монет
  сумарно вимагають кількох днів стабільної гри (щоденні завдання —
  основне стабільне джерело після того, як усі рівні вже "3-зіркові").
  Це відповідає меті плану: швидка ПЕРША покупка, але не миттєве
  "все і одразу".
- Чи дають перегони непропорційно багато: Чемпіонський (80 монет/
  перемога) — найбільше разове джерело в грі, але він (а) заблокований
  до рівня героя 5 АБО перемоги в Пригодницькому, і (б) найважчий
  режим (9 раундів, 4с на відповідь, "гумове" наздоганяння суперників
  найслабше — catchup.max лише +3). Вища нагорода за складніший,
  свідомо відкладений режим — вважаємо це навмисним дизайном, не
  дисбалансом; фарм-захист (як у Тренувального) для нього не потрібен,
  бо сам режим і так вимагає реального результату щоразу.
- Лабіринт і Пам'ять НЕ мають захисту від фарму (на відміну від
  Тренувального заїзду) — технічно повторювані необмежено. Залишено
  як є: обидва режими вимагають кілька реальних хвилин гри й
  правильних відповідей за прохід (не миттєвий тап), тож це нарівні з
  рештою "заробити монети граючи", а не експлойт. Якщо реальні дані
  після запуску покажуть інше — сюди варто буде повернутись.
- Чи фармиться daily reward зміною системного часу пристрою: технічно
  так — `ensureDaily()`/`pickDailyQuestIds()` (progress.js, rewards.js)
  визначають "новий день" виключно через `new Date()` на пристрої,
  без звірки із сервером. Це відома, практично неусувна властивість
  БУДЬ-ЯКОЇ офлайн-first гри з щоденними нагородами без бекенда (а
  бекенд/акаунти цей проєкт свідомо відкладає — розділ 10) — не
  унікальна вада цієї гри, і для дитячої математичної гри без
  змагального рейтингу це не критично. Не виправлено, свідомо.

Код-числа НЕ змінювались цим заходом — реальних дисбалансів (типу
"можна купити все за один вечір" чи "формула дає 0/від'ємні монети")
не знайдено, лише задокументовано те, що вже було в коді.

## 20. Щоденні завдання треба зробити різноманітнішими

Зараз три завдання постійно однакові. Після кількох днів вони перестануть
бути цікавими.

Створити пул із 15–20 варіантів:

1. Дай 15 правильних відповідей.
2. Пройди 2 рівні.
3. Знайди 3 пари у пам'яті.
4. Відкрий скриню в лабіринті.
5. Посідай перше або друге місце в перегонах.
6. Дай 5 відповідей поспіль.
7. Повтори таблицю на 7.
8. Переможи без підказки.
9. Знайди секретний шлях.
10. Виконай 10 прикладів менше ніж за 4 секунди.
11. Покращ свій рекорд.
12. Виправ 3 слабкі приклади.

Щодня випадково обирати три завдання, але гарантувати:

- одне просте;
- одне середнє;
- одне пов'язане з тренуванням.

## 21. Досягнення також потрібно розширити

Наявні 8 досягнень хороші для старту, але їх швидко завершать.

Додати категорії:

**Навчання**

- 100 правильних відповідей.
- 500 правильних відповідей.
- Майстер таблиці на 7.
- Усі таблиці понад 80% mastery.
- 20 правильних поспіль.

**Пригоди**

- 10 відкритих скринь.
- 5 секретних шляхів.
- 10 перемог у перегонах.
- Перемога у чемпіонському заїзді.

**Наполегливість**

- Повернувся після поразки й переміг.
- 7-денний streak.
- 30 навчальних сесій.

**Колекція**

- Перший придбаний аватар.
- 5 аватарів.
- Уся колекція.

Не всі досягнення повинні давати монети. Частина може давати:

- декоративний титул;
- рамку;
- ефект;
- спеціальний значок.

Реалізовано (04.08.2026): додано всі 15 запланованих досягнень у 4
категоріях (rewards.js: BADGES, тепер 23 разом із початковими 8) —
Навчання (100/500 правильних відповідей, майстер таблиці на 7, усі
таблиці ≥80% mastery, 20 відповідей поспіль), Пригоди (10 скринь, 5
секретних шляхів, 10 перемог у перегонах, перемога в Чемпіонському
заїзді), Наполегливість (повернувся після поразки й переміг, 7-денний
streak, 30 тренувань), Колекція (перша покупка, 5 аватарів, уся
колекція). Жодне не дає монет/XP — рамки/титули/ефекти з плану
свідомо НЕ впроваджені як окрема система (шоу-стопер для цього заходу:
в магазині досі лише аватари), тож усі нові бейджі суто декоративні,
як і 8 початкових.

Технічно значуще: раніше перевірка бейджів (`BADGES.filter(...)`)
існувала ЛИШЕ всередині completeLevel() — бейдж за дію в лабіринті чи
перегонах формально ставав "true", але спливав лише при НАСТУПНІЙ
перемозі в бою, без видимої причини для гравця. Винесено у спільну
`checkBadges()` (App.jsx) і викликано з УСІХ місць, що нараховують
прогрес: completeLevel, completeMaze, completeRace, rewardPractice
(пам'ять + "Мої знання"), recordFact, purchaseAvatar — новий бейдж
тепер спливає одразу в тому режимі, де його заслужили.

Нові лічильники в progress.js (з фолбеком для старих збережень через
migrateProgress): `answerStreak`/`bestAnswerStreak` (глобальна серія
відповідей поспіль, окремо від per-fact correctStreak у mastery.js),
`totalChestsOpened`/`totalSecretsFound` (лабіринт, НАЗАВЖДИ, на
відміну від daily.mazeChestsToday), `totalRaceWins`/`championRaceWon`,
`lastFailedLevelId`/`hadComeback` (для "повернувся після поразки" —
onGameOver раніше взагалі не торкався progress, лише слав аналітику).
"30 навчальних сесій" — без нового лічильника, наближено як унікальні
пройдені рівні + mazeCompletions + raceCompletions (обидва вже рахують
повтори).

Верифіковано: 7 нових тестів у tests/logic.test.mjs (унікальність id,
чистий прогрес не розблоковує нічого, межові значення 100/500/20,
колекція аватарів, table7_master вимагає реального майстерства, не
лише спроб) — `npm test` = 41/41. i18n: 15 nameKey у achievements.json
на всіх 8 мовах (JSON valid, паритет ключів, без залишку кирилиці в
не-uk файлах).

## 22. Контентний редактор

Коли з'являться дроби, рівняння й геометрія, рівні не повинні бути жорстко
зашиті в компонентах. Зараз варто перейти до data-driven структури:

```json
{
  "id": "forest-01",
  "regionId": "forest",
  "title": "Перші кроки",
  "skills": ["multiply-2", "multiply-3"],
  "questionTypes": [
    "direct",
    "missing-factor",
    "word-problem"
  ],
  "enemyId": "forest-worm",
  "questionCount": 8,
  "timeLimit": 10,
  "rewards": {
    "coins": 20,
    "xp": 35
  }
}
```

Генератори завдань:

- `multiplication`
- `division`
- `missingFactor`
- `comparison`
- `expressionChain`
- `wordProblem`

У майбутньому:

- `fractions`
- `decimals`
- `equations`
- `percentages`
- `geometry`
- `coordinates`

Так додавання нових 100 рівнів не вимагатиме копіювати логіку кожного
екрана.

## 23. Архітектура, яку варто мати до масштабування

Розділити систему на модулі:

```
src/
  audio/
    AudioManager
    music
    sfx
  content/
    regions
    levels
    questionGenerators
    hints
  game/
    battle
    memory
    maze
    race
  progression/
    xp
    rewards
    achievements
    dailyQuests
    mastery
  persistence/
    saveService
    migrations
    backup
    importExport
  analytics/
    events
    provider
  accessibility/
    focus
    reducedMotion
  components/
    ui
    modals
    buttons
    cards
```

Особливо важливо, щоб:

- генерація завдань не була всередині UI;
- нагороди рахувалися в одному сервісі;
- localStorage не викликався напряму з десятків компонентів;
- аудіо керувалося одним AudioManager;
- усі модалки використовували один базовий компонент;
- усі кнопки використовували спільні варіанти primary і secondary.

## 24. План запуску на 6 етапів

### Етап 1 — стабілізація

- заморозити нові візуальні функції;
- провести аудит усіх flows;
- додати saveVersion;
- міграції;
- backup;
- ErrorBoundary;
- захист від подвійних нагород і покупок;
- виправити responsive bugs.

### Етап 2 — навчальна якість

- діагностичний старт;
- adaptive mastery;
- слабкі приклади;
- пояснення помилок;
- різні типи питань;
- екран «Мої знання».

### Етап 3 — launch shell

- onboarding;
- сторінка «Про гру»;
- політика приватності;
- умови;
- контакти;
- favicon;
- метадані;
- OG-зображення;
- номер версії;
- changelog.

### Етап 4 — продуктивність і PWA

- оптимізація зображень;
- lazy loading;
- поділ bundle;
- service worker;
- manifest;
- install prompt;
- offline fallback;
- тест Core Web Vitals.

### Етап 5 — закрита бета

Як учитель, ти маєш велику перевагу: можеш протестувати гру на реальних
дітях.

Провести тест приблизно так:

- 5–10 дітей без твоїх підказок;
- 2–3 учителі;
- кілька батьків;
- різні телефони.

Спостерігати:

- де дитина зупинилася;
- що не зрозуміла;
- де випадково натиснула;
- який текст не прочитала;
- коли втратила інтерес;
- чи зрозуміла монети й XP;
- чи знайшла тренування;
- чи змогла повернутися після помилки.

Не пояснювати інтерфейс під час першої спроби. Якщо доводиться
пояснювати — це проблема інтерфейсу або onboarding.

### Етап 6 — публічна бета

Запускати як:

> Королівство Математики — публічна бета
> Перший розділ: таблиця множення 2–9

На сторінці показати:

- що вже працює;
- що планується;
- кнопку «Повідомити про проблему»;
- форму короткого відгуку;
- номер версії.

## 25. Які метрики дивитися після запуску

Не потрібно відразу мати складну бізнес-аналітику.

**Перший запуск**

- скільки почали tutorial;
- скільки завершили;
- скільки почали перший рівень;
- скільки завершили перший рівень.

**Навчання**

- середня точність;
- середній час відповіді;
- найскладніші пари;
- частота використання підказок;
- кількість повторних спроб.

**Утримання**

- скільки повернулися наступного дня;
- скільки дійшли до другого регіону;
- скільки відкрили магазин;
- скільки купили перший аватар;
- які тренування використовують.

**Технічні**

- помилки JavaScript;
- невдалі завантаження ресурсів;
- проблеми з save;
- повільні екрани;
- моделі пристроїв із проблемами.

## 26. Критерії готовності до публічної бети

Гру можна відкривати широкій аудиторії, коли:

- новий користувач проходить перший рівень без пояснень автора;
- прогрес переживає оновлення структури save;
- є експорт та імпорт;
- покупки не дублюються;
- нагороди не можна випадково забрати двічі;
- усі кнопки працюють на ширині 320 px;
- немає горизонтального scroll;
- немає обрізаних сердець, модалок і заголовків;
- звук можна повністю вимкнути;
- музика не запускається кілька разів;
- гра не падає без мережі;
- є зрозумілий offline/error screen;
- є політика приватності;
- є сторінка контакту;
- є PWA-іконки й favicon;
- пройдені основні сценарії хоча б на Android та iPhone;
- немає відомих P0 і P1 багів;
- щонайменше кілька дітей змогли самостійно завершити сесію.

Фінальна звірка (04.08.2026) — пройдено пункт за пунктом проти коду:

- Новий користувач без пояснень / прогрес переживає міграцію save /
  export-import / покупки не дублюються / нагороди не забираються
  двічі / звук вимикається повністю / музика не запускається двічі /
  гра не падає без мережі / зрозумілий offline-екран / privacy /
  contact / PWA-іконки — усе підтверджено в коді (onboarding,
  progress.js migrateSave + 41 unit-тест, ShopScreen/
  AvatarPurchaseModal, checkQuests, MenuScreen toggle, music.js
  singleton-guard на sourceNode, apiClient NETWORK_ERROR + офлайн-перша
  архітектура, offline.html, ErrorBoundary, legal-сторінки, manifest).
- 320px / горизонтальний скрол / обрізані елементи — перевірено в
  мобільному аудиті (розділ 18) цієї сесії, регресій не внесено
  жодною з наступних змін (перевірено повторно: жодного небезпечного
  min-width, лише max-w зі зменшенням під менші екрани).
- НЕ підтверджено з цієї пісочниці (потребує реального пристрою/дітей,
  не код-рівнева перевірка): "пройдені сценарії на Android/iPhone" і
  "кілька дітей самостійно завершили сесію" — обидва явно лишаються
  відкритими пунктами, як і зазначено в розділі 18. Це єдине, що
  реально блокує формальне "готово до публічної бети" — увесь
  код-рівневий чек-лист виконано.
- "Немає відомих P0/P1 багів" — жодного відомого відкритого не
  знайдено за весь код-рівневий аудит цієї сесії (розділи 12, 13, 16,
  17, 18, 19, 21); природно, це про ВІДОМІ баги, а не гарантію
  відсутності неявних.

---

## 27. Монетизація

Повна модель — в окремому документі `rekomendovana_model_monetyzatsii_math_rpg.md`.
Тут лише коротко, як вона стикується з рештою плану.

**Не робити зараз.** Розділ "Що НЕ додавати зараз" нижче явно виключає
платні покупки з першого релізу — монетизація йде вже після нього, коли
є перший робочий безкоштовний продукт, реальні гравці й відгуки.

Коротко про рекомендовану модель:

- безплатне завантаження, без реклами;
- безплатна частина: перший регіон, 3–4 рівні, базове тренування, 1–2
  аватари, локальний прогрес;
- одноразова покупка **"Відкрити повну пригоду"** відкриває решту
  регіонів/режимів, хмарне збереження, синхронізацію між пристроями,
  сімейну статистику;
- рекомендована стартова ціна — 199 грн одноразово;
- покупка — лише через окремий батьківський розділ, за **parental
  gate** (не проста для випадкового натискання дитиною);
- кнопка "Відновити покупку" для перевстановлення/зміни пристрою.

**Залежності від інших розділів цього плану:**

- хмарне збереження й синхронізація в повній версії вимагають
  серверного прогресу — тобто спершу розділ 9 (збереження) і розділ 10
  (акаунти, зараз свідомо відкладені, `src/config.js:AUTH_ENABLED`) мають
  бути доведені до робочого стану;
- батьківський розділ і parental gate логічно лягають туди ж, де вже
  планується розділ 8 ("Додати окремий режим для дорослого");
- Android-публікація (`android-play-store-plan.md`) зараз рахує гру як
  `Price: Free` без платних розблокувань — перед стадією монетизації
  цей крок і Google Play Console (in-app purchase / paid app) потрібно
  буде переглянути окремо.

Порядок: спершу безкоштовний реліз і розділи 1–26 цього плану, потім
акаунти й серверна синхронізація, і лише тоді — монетизація за моделлю
з `rekomendovana_model_monetyzatsii_math_rpg.md`.

---

## Що НЕ додавати зараз

Перед першим запуском не варто витрачати час на:

- чат між дітьми;
- глобальні рейтинги;
- складні акаунти;
- соціальні профілі;
- рекламу;
- платні покупки;
- мультиплеєр;
- усі 100+ рівнів;
- ще десятки косметичних предметів;
- нескінченне полірування рамок.

Ці речі не зроблять першу версію значно кориснішою. Надійний прогрес,
onboarding, навчальні пояснення та реальне тестування — зроблять.

## Рекомендована версія для першого запуску

> **Королівство Математики — Розділ 1: Магія множення**

У комплекті:

- 12 сюжетних рівнів;
- адаптивна складність;
- 3 тренувальні режими;
- діагностичний старт;
- пояснення помилок;
- екран слабких і сильних прикладів;
- магазин аватарів;
- досягнення;
- різноманітні daily quests;
- локальний прогрес із backup та export/import;
- PWA;
- музика й звуки;
- без акаунтів;
- без реклами;
- без збору персональних даних;
- простий екран для батьків і вчителя.

Це вже буде продукт, який не соромно давати дітям, учителям і батькам. А
наступні математичні регіони можна випускати поступово як великі
оновлення, не затримуючи перший реальний запуск.
